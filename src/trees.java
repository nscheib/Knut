import java.awt.*;


private class trees {
    int x = 10;
    int y = 10;
    int size = 10;
    int discount = ((int)(Math.random()*15)+1);
    boolean choppedDown = false;

    public static tree(int x, int y, int size){

    }

    public static treeDiscountChopDown(int x, int size, int discount){

    }

    public static chopDown(int x, int y,){
        boolean
    }

    public static void drawTree(Graphics g){
        Graphics2D g2d = (Graphics2D)g;
        g2d.setRenderingHint(RenderingHints.KEY_ANTIALIASING, RenderingHints.VALUE_ANTIALIAS_ON);

        g2d.drawOval(100, 100, 300, 500);
        g2d.setColor(Color.GREEN);
    }

}
